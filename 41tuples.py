#tuples also a list but can't be changed like list
#tuples has small brackets
numbers=(1,2,1,1,3)
print(numbers.count(1))
print(numbers[0])
#can get info but not change and its rarely used
#it's necessary when list shouldn't be changed